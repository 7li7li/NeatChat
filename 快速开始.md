# NextChat 快速开始指南

## 📋 配置清单

已为你创建以下文件：
- ✅ `ecosystem.config.js` - PM2 配置文件
- ✅ `web.config` - IIS 反向代理配置
- ✅ `deploy.bat` - 一键部署脚本
- ✅ `pm2-manage.bat` - PM2 管理工具
- ✅ `.env.local` - 环境变量配置
- ✅ `logs/` - 日志目录

## 🚀 快速部署（3 步完成）

### 第 1 步：配置 API 密钥

编辑 `.env.local` 文件，至少配置以下内容：

```env
# 必填：你的 OpenAI API Key
OPENAI_API_KEY=sk-your-actual-api-key-here

# 可选：设置访问密码（推荐）
CODE=your-access-password

# 可选：如果需要代理
PROXY_URL=http://localhost:7890
```

### 第 2 步：运行部署脚本

双击运行 `deploy.bat`，脚本会自动：
1. 安装依赖
2. 构建项目
3. 启动 PM2 服务

等待完成即可！

### 第 3 步：配置 IIS（可选）

如果需要通过 IIS 访问，请按照 `IIS配置说明.md` 进行配置。

**必需组件**：
- URL Rewrite 模块: https://www.iis.net/downloads/microsoft/url-rewrite
- Application Request Routing: https://www.iis.net/downloads/microsoft/application-request-routing

## 🎯 访问应用

部署完成后，可以通过以下方式访问：

- **直接访问**: http://localhost:3000
- **通过 IIS**: http://localhost（配置 IIS 后）

## 🛠️ 常用操作

### 查看应用状态
```cmd
pm2 status
```

### 查看实时日志
```cmd
pm2 logs nextchat
```

### 重启应用
```cmd
pm2 restart nextchat
```

### 停止应用
```cmd
pm2 stop nextchat
```

### 使用管理工具
双击运行 `pm2-manage.bat`，提供图形化菜单操作。

## 📊 验证部署

1. 检查 PM2 状态：
   ```cmd
   pm2 status
   ```
   应该看到 `nextchat` 状态为 `online`

2. 访问应用：
   打开浏览器访问 http://localhost:3000

3. 检查日志：
   ```cmd
   pm2 logs nextchat --lines 20
   ```

## 🔧 故障排查

### 应用无法启动

1. 检查依赖是否安装：
   ```cmd
   yarn install
   ```

2. 检查构建是否成功：
   ```cmd
   yarn build
   ```

3. 查看错误日志：
   ```cmd
   pm2 logs nextchat --err
   ```

### 端口被占用

修改 `ecosystem.config.js` 中的端口：
```javascript
env: {
  PORT: 3001  // 改为其他端口
}
```

然后重启：
```cmd
pm2 restart nextchat
```

### IIS 502 错误

1. 确认 PM2 应用正在运行：
   ```cmd
   pm2 status
   ```

2. 确认端口匹配（PM2 和 web.config 中的端口要一致）

3. 检查 ARR 代理是否启用

## 🔄 更新应用

当代码更新后，重新运行部署脚本：
```cmd
deploy.bat
```

或手动执行：
```cmd
git pull
yarn install
yarn build
pm2 restart nextchat
```

## 🌟 开机自启（可选）

### 方法 1：使用 pm2-installer（推荐）

1. 下载: https://github.com/jessety/pm2-installer/releases
2. 运行安装程序
3. PM2 将作为 Windows 服务自动启动

### 方法 2：使用任务计划程序

创建开机启动任务，运行：
```cmd
pm2 resurrect
```

## 📞 需要帮助？

- 查看详细 IIS 配置: `IIS配置说明.md`
- PM2 官方文档: https://pm2.keymetrics.io/
- NextChat 项目: https://github.com/ChatGPTNextWeb/ChatGPT-Next-Web

## ⚙️ 高级配置

### 多实例部署（利用多核 CPU）

编辑 `ecosystem.config.js`:
```javascript
instances: 'max',  // 使用所有 CPU 核心
exec_mode: 'cluster'
```

### 配置 HTTPS

在 IIS 中配置 SSL 证书，然后修改网站绑定为 HTTPS。

### 自定义域名

1. 在 IIS 网站绑定中添加主机名
2. 配置 DNS 解析
3. 建议配置 HTTPS

---

**祝你使用愉快！** 🎉
